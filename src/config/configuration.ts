import { ENV_CONFIG } from "@/config/env.config";
import { EMAIL_CONFIG } from "@/config/email.config";
import { RELAY_DB_CONFIG } from "@/config/database.config";

export default [ENV_CONFIG, EMAIL_CONFIG, RELAY_DB_CONFIG];
